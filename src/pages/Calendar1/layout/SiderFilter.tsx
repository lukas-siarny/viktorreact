import React from 'react'
import { useTranslation } from 'react-i18next'
import cx from 'classnames'

const SiderFilter = () => {
	const [t] = useTranslation()

	return <div className='nc-sider-filter'>{'Calednar'}</div>
}

export default SiderFilter
